namespace MafiaOnline
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();
            //Routing.RegisterRoute("HostPage", typeof(HostPage));
        }
    }
}
